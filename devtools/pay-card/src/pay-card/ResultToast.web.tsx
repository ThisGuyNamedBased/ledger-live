/**
 * The Card session controls are mobile-only for now, so the web panel shows no result banner. This
 * variant exists to keep the platform pair complete.
 */
export function ResultToast() {
  return null;
}
